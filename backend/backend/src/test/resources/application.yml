spring:
    profiles.active: test
---
spring:
    profiles: test
spring.datasource:
    createdb: create-drop
    url: jdbc:h2:mem:testdb
spring.jpa:
    show-sql: true
    database-platform: org.hibernate.spatial.dialect.h2geodb.GeoDBDialect
